class Quiz
{
    static void Randomize()
    {
        //determine how many characters are in the string
        //get a percentage based on _difficultyvalue
        //black out that number of characters in a temporary string
        //print the temporary string 
        //get user input and compare it (?)
        //if correct, increment _difficultyvalue and continue until completely blacked out
        //if not, repeat without incrementing.
    }
}